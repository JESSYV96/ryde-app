# Base URL of apps/server (payment links + vehicle recognition).
EXPO_PUBLIC_SERVER_URL=http://localhost:4000

# Deprecated alias, still read as a fallback by shared/api/serverUrl.ts.
EXPO_PUBLIC_PAYMENT_API_URL=http://localhost:4000
